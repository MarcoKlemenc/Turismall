@model Turismall.Models.Viaje

@{
    ViewData["Title"] = "Detalles";
}

<h2>Detalles</h2>

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>
        <dd>
            @if (String.IsNullOrEmpty(@Model.Descripcion))
                {
                <p>No hay descripción.</p>
                } else
                {
                @Html.DisplayFor(model => model.Descripcion)
                }


        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model.ID"><span class="glyphicon glyphicon-pencil" /></a>
    <a asp-action="Index" class="btn btn-default"><span class="glyphicon glyphicon-arrow-left" /></a>
</div>
